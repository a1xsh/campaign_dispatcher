<% if campaign.pending? %>
  <%= button_to "Start Dispatch", start_campaign_path(campaign),
        method: :post,
        class: "px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700" %>
<% end %>
